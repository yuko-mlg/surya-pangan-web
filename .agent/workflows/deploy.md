---
description: Deploy changes to GitHub (Auto-run enabled)
---

// turbo-all
1. Add all changes to git
`git add .`

2. Commit changes with a message (default: "update: apply changes")
`git commit -m "update: apply changes"`

3. Push to main branch
`git push origin main`
